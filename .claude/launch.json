{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "Static Site",
      "runtimeExecutable": "/usr/local/bin/python3",
      "runtimeArgs": ["-c", "import http.server, socketserver, os; os.chdir('/tmp/discover-beta'); socketserver.TCPServer.allow_reuse_address = True; httpd = socketserver.TCPServer(('', 8000), http.server.SimpleHTTPRequestHandler); httpd.serve_forever()"],
      "port": 8000
    }
  ]
}
